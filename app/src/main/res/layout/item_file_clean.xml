<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:fresco="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@mipmap/bg_item_file"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:layout_marginBottom="12dp"
    android:padding="12dp">

    <!-- 使用 Fresco 的 SimpleDraweeView 替代 ImageView -->
    <com.facebook.drawee.view.SimpleDraweeView
        android:id="@+id/iv_file_icon"
        android:layout_width="20dp"
        android:layout_height="20dp"
        fresco:placeholderImage="@mipmap/ic_item_file"
        fresco:failureImage="@mipmap/ic_item_file"
        fresco:actualImageScaleType="centerCrop"
        fresco:roundedCornerRadius="4dp" />

    <LinearLayout
        android:id="@+id/layout_file_info"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="12dp"
        android:layout_weight="1"
        android:orientation="vertical">

        <androidx.appcompat.widget.AppCompatTextView
            android:id="@+id/tv_file_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:maxLines="2"
            android:text="File Name"
            android:textColor="#FFFFFFFF"
            android:textSize="14sp" />

        <androidx.appcompat.widget.AppCompatTextView
            android:id="@+id/tv_file_size"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="0 MB"
            android:textColor="#FF7A7A7A"
            android:textSize="12sp" />

    </LinearLayout>

    <ImageView
        android:id="@+id/iv_select_status"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:background="@mipmap/ic_not_selete"
        android:button="@null" />

</LinearLayout>